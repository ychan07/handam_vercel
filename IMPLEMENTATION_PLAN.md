# 한담 기능 구현 계획

## 목표

`prototype-claude.html`에서 옮긴 웹앱을 기준으로, 문서에 적힌 핵심 기능을 실제 서비스 구조로 확장한다. 현재 단계에서는 외부 API 없이 사용자가 누르는 모든 주요 버튼이 다음 상태나 페이지로 이동하도록 만들고, 이후 API/DB 연동이 가능한 경계까지 정리한다.

## 기능별 구현 계획

### 1. OCR 기반 일기 요약 및 저장

- 입력 방식은 `촬영하기`, `보관함`, `직접 입력하기` 세 가지로 둔다.
- `촬영하기`와 `보관함`은 현재 프로토타입처럼 OCR 처리 상태를 거쳐 인식 결과 화면으로 이동한다.
- `직접 입력하기`는 별도 작성 페이지로 이동해 제목, 본문, 감정을 입력하고 저장할 수 있게 한다.
- 실제 서비스 단계에서는 Camera/File input으로 이미지를 받고, Naver Clova OCR API에 전송한다.
- OCR 결과는 Gemini API로 오타 교정, 정규화, 한 줄 요약을 생성한다.
- 저장소는 1차 MVP에서는 `localStorage` 또는 IndexedDB로 시작하고, 검색/필터가 필요해지면 SQLite Wasm 또는 Dexie 기반 IndexedDB로 확장한다.

### 2. LLM 기반 글감 추천

- MVP에서는 미리 준비한 글감 세트를 섞어 보여주는 방식으로 구현한다.
- `다른 글감 추천받기` 버튼은 추천 카드 3개를 즉시 교체한다.
- 추천 카드 선택 시 일기 작성 화면으로 이동하고, 선택한 글감이 작성 화면의 안내 문구로 반영되도록 한다.
- 실제 서비스 단계에서는 최근 일기 요약, 감정 태그, 자주 등장한 키워드를 Gemini API에 전달해 개인화 글감을 생성한다.
- 비용 제어를 위해 기본 모델은 Gemini로 두고, 요금제에 따라 성능 단계만 노출한다.

### 3. 운세 기능

- 사용자는 생년월일과 음력 여부를 입력한다.
- MVP에서는 입력 완료 시 운세 점수/문구를 갱신하는 시뮬레이션으로 동작한다.
- 실제 서비스 단계에서는 운세 API에 생년월일을 보내고, 응답 데이터를 총운, 분야별 운, 행운의 색/숫자로 파싱한다.
- API 응답 실패 시 기본 운세 문구와 재시도 안내를 제공한다.

### 4. 설정과 AI 품질 선택

- `요약 정확도`는 표준, 고급, 최고 중 하나만 선택되도록 한다.
- `AI 페르소나`는 따뜻한 공감형, 담백한 정리형 중 하나만 선택되도록 한다.
- MVP에서는 선택 상태를 화면에 반영하고 브라우저 저장소에 보관한다.
- 실제 서비스 단계에서는 선택값을 요약 프롬프트와 모델 라우팅에 반영한다.

### 5. 인증 및 계정

- `비밀번호 변경`은 별도 페이지에서 현재 비밀번호, 새 비밀번호, 확인값을 입력받는다.
- `로그아웃`은 로그인 페이지로 이동하고, MVP에서는 세션 종료 토스트를 보여준다.
- 실제 서비스 단계에서는 JWT 기반 로그인과 `httpOnly`, `Secure` 쿠키 기반 Refresh Token 구조로 확장한다.
- OAuth는 카카오/구글 추가를 고려하되, MVP에서는 자체 인증 흐름을 먼저 완성한다.

### 6. 백업 및 내보내기

- `일기 백업 · 내보내기`는 별도 페이지에서 로컬 저장, JSON 내보내기, 서버 백업 상태를 보여준다.
- MVP에서는 샘플 백업 완료 상태와 내보내기 버튼 피드백을 제공한다.
- 실제 서비스 단계에서는 로컬 DB 데이터를 JSON/CSV로 내보내고, 사용자가 동의할 때만 서버 백업을 실행한다.

## 현재 프로토타입에서 바로 구현할 항목

- `전체보기`를 기록 목록 페이지로 연결한다.
- 최근 기록 항목을 기록 상세 페이지로 연결한다.
- `직접 입력하기`를 직접 작성 페이지로 연결한다.
- `다른 글감 추천받기`를 실제 추천 카드 교체로 구현한다.
- 설정의 세그먼트 버튼 선택 상태를 동작하게 한다.
- `비밀번호 변경`, `일기 백업 · 내보내기`, `로그아웃`을 각각 페이지로 연결한다.
- 생년월일 바텀시트의 `운세 다시 보기`는 운세 갱신 피드백을 주도록 한다.

## 향후 구현 순서

1. IndexedDB 기반 일기 저장 구조를 만든다.
2. 직접 입력 저장 데이터를 홈/전체보기/상세에 반영한다.
3. Clova OCR API 연결과 이미지 업로드 UI를 붙인다.
4. Gemini 요약 API와 프롬프트 설정을 연결한다.
5. 운세 API 요청/파싱 레이어를 추가한다.
6. 인증, 백업, PWA 오프라인 캐시를 순서대로 붙인다.
